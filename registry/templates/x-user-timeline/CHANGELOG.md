# Changelog

## 1.1.0 — 2026-09-17

- 新增 `limit`（默认 10，最多 100）；`maxScrolls=0`（默认）时按 limit 自动推导，滚动直到取够或无增长。

## 1.0.0

- 首个版本：复用 x-search 的推文选择器体系（article[data-testid=tweet]），真机验证 elonmusk 时间线 4 条推文（2026-09-07 macOS）。
